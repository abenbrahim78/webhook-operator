---
# Use the 'create api' subcommand to add watches to this file.
- version: v1alpha1
  group: middleware.middleware.operator.com
  kind: Webhook
  playbook: playbooks/webhook.yml
#+kubebuilder:scaffold:watch
